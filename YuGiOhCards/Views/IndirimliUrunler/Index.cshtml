@model IEnumerable<YuGiOhCards.Models.IndirimliUrunler>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Urun)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oran)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Baslangic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bitis)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DigerKampanya)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Urun.Ad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Oran)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Baslangic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bitis)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DigerKampanya)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
